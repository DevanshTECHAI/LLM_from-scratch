# LLM_from-scratch

1. Before running anything, install dependencies:
   pip install torch sentencepiece tqdm
2. python tokenizer.py
3.python main.py --DEVICE cpu --mode train
4.python main.py --DEVICE cpu --mode inference
